"""Population plugins: the star-class-specific parts of the search.

A plugin supplies only what genuinely differs between stellar populations — the natural
photospheric baseline and the natural-explanation regimes. Everything else comes from
`core`. Add a new search (e.g. evolved giants) by writing a new plugin here, not by
forking the pipeline.
"""
